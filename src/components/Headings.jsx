import React from 'react';

const Headings = () => (
  <div className="headings">
    <h1 className="headings-title">
      Office <span className="highlight">Finder</span>
    </h1>
    <h4 className="headings-text">Choose an location</h4>
  </div>
);

export default Headings;
